.recent-notes {
  & > h3 {
    margin: 4rem 0 1rem 0;
    font-size: 1.5rem;
    border-bottom: 2px solid var(--tertiary);
    display: inline-block;
    padding-bottom: 0.3rem;
  }

  & > ul.recent-ul {
    list-style: none;
    margin-top: 1rem;
    padding-left: 0;

    & > li {
      margin: 1rem 0;
      .section > .desc > h3 > a {
        background-color: transparent;
      }

      .section > .meta {
        margin: 0 0 0.5rem 0;
        opacity: 0.6;
      }
    }

    // Enhanced layout for recent items: spacing, inline date, tags, and separator line
    & > li.recent-li {
      margin: 0;
      padding: 1rem 0 1.2rem 0;

      &:not(:last-child)::after {
        content: "";
        display: block;
        width: 100%;
        height: 1px;
        background-color: var(--lightgray);
        margin-top: 1rem;
      }

      .recent-header {
        display: flex;
        align-items: baseline;
        gap: 0.75rem;
        flex-wrap: wrap;

        .recent-title {
          margin: 0;
          & > h3 {
            margin: 0;
          }
          & > h3 > a {
            background-color: transparent !important;
            padding: 0 !important;
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--dark);
          }
        }

        .recent-date {
          opacity: 0.6;
          font-size: 0.95rem;
          white-space: nowrap;
          color: var(--darkgray);
        }
      }

      .section > ul.tags {
        list-style: none;
        margin: 0.5rem 0 0 0;
        padding: 0;
        display: flex;
        flex-wrap: wrap;
        gap: 0.4rem 0.6rem;

        & > li {
          margin: 0;
        }
      }
    }
  }
}
